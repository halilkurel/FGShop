@using FGShop.WebUI.Models.ProductModels
@model GetByProductIdModel

<div class="product-page-details">
    <h3>@Model.Data.ProductName</h3>
</div>
<div class="product-price">
    @Model.Data.Price TL
</div>

<a href="@Url.Action("UpdateProduct", "Product", new { area = "Admin", id = ViewBag.Id })" class="btn btn-primary mt-2">Ürün adı ve Fiyat Güncelle</a>